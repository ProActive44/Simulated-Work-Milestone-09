
{
  "name": "instagram-webapp",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm i && npm start",
    "backend": "cd backend && npm i && curl --output backend/data/test_db.sqlite https://kq-storage.s3.ap-south-1.amazonaws.com/test_db+(1).sqlite && node app.js",
    "test:serve":"cd frontend && npm i && npm run test:serve"
  }
}
